---
feature: {{feature_slug}}
layer: coverage_report
anchor: {{feature_slug}}/coverage_report/root
title: {{app_name}} — {{feature_title}} Coverage Report
last_updated: '{{date_iso}}'
generated_at: {{generated_at_iso}}
generated_by: coverage_report.py v1
scope_version: {{scope_version}}
status: draft                                      # → sign_off_pass | sign_off_fail after PM review

captured:
  count: {{captured_count}}
  anchors:
    - {{captured_anchor_1}}
    - {{captured_anchor_2}}

gaps:                                              # IN scope but NOT captured
  - anchor: {{gap_anchor_1}}
    reason: "{{gap_reason_1}}"

drift:                                             # CAPTURED but NOT declared in scope
  - capture_label: {{drift_label_1}}
    hash: {{drift_hash_1}}
    cluster_guess: "{{drift_cluster_guess}}"
    reason_guess: "{{drift_reason_guess}}"

unknowns_resolved: []                              # PM appends manually after review
decisions: []                                      # PM action items

related:
  - {{feature_slug}}/scope/root
  - {{feature_slug}}/observations/root             # forward-ref (may not exist yet)
---

# {{app_name}} — {{feature_title}} Coverage Report

> **Layer 4 / 4 — capture phase audit.**
> File này được generate tự động bởi `coverage_report.py` SAU khi capture xong,
> diff giữa scope.md (PM contract) vs nav_graph.json + dumps thực tế.
>
> PM review để quyết định:
> - **Gap** → re-capture, hay revise scope drop yêu cầu này?
> - **Drift** → add vào scope cluster nào, hay drop?
> - **Unknown chưa resolved** → trả lời ngay
>
> Sau review, set `status: sign_off_pass` (cho phép spec-writer chạy) hoặc
> `sign_off_fail` (block spec-writer, cần re-capture).

---

## 1. Summary

| Metric | Count |
|---|---|
| Must-visit captured | {{must_captured}} / {{must_total}} |
| Optional captured | {{opt_captured}} / {{opt_total}} |
| Drift (out-of-scope captures) | {{drift_count}} |
| Gap (in-scope misses) | {{gap_count}} |
| Unknowns unresolved | {{unknowns_unresolved}} |

**Status**: _(PM fills after review)_ — `sign_off_pass` hoặc `sign_off_fail`.

---

## 2. Captured (matches scope)

Anchors trong scope đã có dump + screenshot:

| Anchor | Cluster | Capture file | Dump file |
|---|---|---|---|
| _(auto-filled by coverage_report.py)_ |  |  |  |

---

## 3. Gaps (in scope but missing)

Mỗi gap = must_visit anchor được declared trong scope nhưng KHÔNG có capture.

### Gap 1: {{gap_anchor_1}}

- **Reason**: {{gap_reason_1}}
- **Suggested fix**: _(eg. "rerun app-explorer with extended timeout for cluster X")_
- **PM decision**: _(fill in: re-capture / revise scope to drop / accept partial)_

---

## 4. Drift (captured but not in scope)

Mỗi drift = screen captured được nhưng KHÔNG declared trong scope.

### Drift 1: {{drift_label_1}}

- **Hash**: `{{drift_hash_1}}`
- **Cluster guess**: {{drift_cluster_guess}}
- **Reason guess**: {{drift_reason_guess}}
- **Capture file**: `{{drift_capture_file_1}}`
- **PM decision**: _(fill in: add to scope cluster X / drop drift / move to out_of_scope)_

---

## 5. PM review

PM trả lời inline + append vào frontmatter `unknowns_resolved` + `decisions`.

### Open scope questions still unresolved

- _(auto-list of `unresolved_questions()` from scope_loader)_

### Action items after review

_(PM fill — these go into `decisions:` frontmatter list)_

- [ ] _(action 1)_
- [ ] _(action 2)_

### Final status

After PM completes review, update frontmatter:

```yaml
status: sign_off_pass    # OR sign_off_fail
```

If `sign_off_fail`, the orchestrator will loop back to capture phase
(re-invoke app-explorer with the gap list as scope).

---
feature: {{feature_slug}}
layer: observations
anchor: {{feature_slug}}/observations/root
title: {{app_name}} — {{feature_title}} Raw Observations
last_updated: '{{date_iso}}'
app_version: {{app_version}}
device: {{device_serial}}
locale: {{locale}}
package: {{package}}
screens:
  # Auto-generated by `python tools/observations_tmpl.py {{feature_slug}}`.
  # Each entry maps a captured screen to its anchor + file paths + section line.
  # Edit the writer agent fills `label` more descriptive + `hash` if needed.
  - anchor: {{feature_slug}}/screen/{{screen_slug}}
    label: "{{screen_label}}"
    activity: {{screen_activity}}
    hash: TODO
    capture_file: {{screens_relpath}}/{{capture_label}}.png
    dump_file: {{dumps_relpath}}/{{capture_label}}.json
    section_line: 12
---

# {{app_name}} — {{feature_title}} Raw Observations

> **Layer 1 / 3 — raw capture transcripts.**
> Mỗi screen có bounds table (auto), Observed behaviour + Notes (writer fill).
> Strings UI verbatim — không paraphrase, giữ nguyên mọi ký tự backtick `'...'`.
> Auto-generated skeleton: `python tools/observations_tmpl.py {{feature_slug}}`.

---

## Screen 01 — {{screen_label}} {#{{feature_slug}}/screen/{{screen_slug}}}

- **Screen ID**: `{{screen_hash}}`
- **Dump**: `{{dumps_relpath}}/{{capture_label}}.json` (+ `.xml` projection)
- **Screenshot**: `{{screens_relpath}}/{{capture_label}}.png`
- **Activity**: `{{screen_activity}}`
- **Entry**: {{entry_action}}
- **Dismiss**: {{dismiss_action}}

### Visible elements

| Class | Bounds | Clickable | Text | Content-desc |
|---|---|---|---|---|
| _(rows auto-rendered from a11y_tree by observations_tmpl.py)_ |  |  |  |  |

### Observed transitions

- _(auto: outgoing nav_graph edges)_

### Observed behaviour

- _(writer fills: scroll behavior, sticky regions, sibling-collapse, side-effects on data)_

### Notes

- _(writer fills: A/B variants, locale quirks, accessibility traps, render anomalies)_

---

## Cross-screen invariants (optional)

- _(invariants observed across the whole feature — eg. "audio mini-player persists",_
  _"streak counter increments at midnight local", "back from any sub-screen returns to landing")_

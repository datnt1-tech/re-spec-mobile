---
feature: {{feature_slug}}
layer: observations
anchor: {{feature_slug}}/observations/root
title: {{app_name}} — {{feature_title}} Raw Observations
last_updated: '{{date_iso}}'
app_version: {{app_version}}
device: {{device_serial}}
locale: {{locale}}
package: {{package}}
screens:
  # Auto-generated bởi `re-spec-observations {{feature_slug}}`.
  # Mỗi entry map 1 captured screen sang anchor + file path + section line.
  # Writer agent sẽ fill `label` mô tả hơn + `hash` nếu cần.
  - anchor: {{feature_slug}}/screen/{{screen_slug}}
    label: "{{screen_label}}"
    activity: {{screen_activity}}
    hash: TODO
    capture_file: {{screens_relpath}}/{{capture_label}}.png
    dump_file: {{dumps_relpath}}/{{capture_label}}.json
    section_line: 12
---

# {{app_name}} — {{feature_title}} Raw Observations

> **Layer 1 / 3 — transcript capture thô.**
> Mỗi screen có bảng bounds (auto), Hành vi quan sát + Note (writer fill).
> UI string nguyên văn — không paraphrase, giữ nguyên mọi ký tự backtick `'...'`.
> Output viết bằng tiếng Việt, technical term giữ tiếng Anh (xem I18N_GLOSSARY).
> Auto-generated skeleton: `re-spec-observations {{feature_slug}}`.

---

## Screen 01 — {{screen_label}} {#{{feature_slug}}/screen/{{screen_slug}}}

- **Screen ID**: `{{screen_hash}}`
- **Dump**: `{{dumps_relpath}}/{{capture_label}}.json` (+ `.xml` projection)
- **Screenshot**: `{{screens_relpath}}/{{capture_label}}.png`
- **Activity**: `{{screen_activity}}`
- **Entry**: {{entry_action}}
- **Dismiss**: {{dismiss_action}}

### Visible elements

| Class | Bounds | Clickable | Text | Content-desc |
|---|---|---|---|---|
| _(row auto-render từ a11y_tree bởi observations_tmpl.py)_ |  |  |  |  |

### Observed transitions

- _(auto: outgoing nav_graph edge)_

### Hành vi quan sát

- _(writer fill: scroll behavior, vùng sticky, sibling-collapse, side-effect lên data)_

### Note

- _(writer fill: variant A/B, quirk locale, trap accessibility, render anomaly)_

---

## Cross-screen invariants (optional)

- _(invariant quan sát qua toàn feature — vd. "audio mini-player giữ persistent",_
  _"streak counter tăng lúc midnight local", "back từ sub-screen bất kỳ về landing")_

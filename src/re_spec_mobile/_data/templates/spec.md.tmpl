---
feature: {{feature_slug}}
layer: flow
anchor: {{feature_slug}}/flow/root
title: {{app_name}} — {{feature_title}} Flow Spec
last_updated: '{{date_iso}}'
app_version: {{app_version}}
device: {{device_label}}
package: {{package}}
status: draft
blocks:
  - id: {{feature_slug}}/block/a
    letter: A
    name: "{{block_a_name}}"
    section_line: 60
    screens: [{{feature_slug}}/screen/{{screen_a_slug}}]
nav_edges:
  - from: {{feature_slug}}/screen/{{screen_a_slug}}
    to: {{feature_slug}}/screen/{{screen_b_slug}}
    trigger: "{{nav_trigger}}"
    block: {{feature_slug}}/block/b
states: []
related:
  - {{feature_slug}}/observations/root
  - {{feature_slug}}/implementation/root
---

# {{app_name}} — {{feature_title}} Flow Specification

**Package**: `{{package}}`
**App version**: `{{app_version}}`
**Activity**: `{{main_activity}}`
**Device tested**: {{device_label}}, locale `{{locale}}`
**Method**: every screen captured via `capture.py` (Portal a11y_tree + screencap + uiautomator-compat XML projection); every UI literal transcribed verbatim. See `{{feature_slug}}_observations.md` for raw per-screen tables.

---

## 1. Overview

{{feature_one_liner}}.

**Functional blocks** ({{block_count}} distinct UI states across {{group_count}} functional groups):

| # | Block | States | Purpose |
|---|---|---|---|
| A | {{block_a_name}} | {{block_a_states}} | {{block_a_purpose}} |
| B | {{block_b_name}} | {{block_b_states}} | {{block_b_purpose}} |

---

## 2. Hard facts before anything else

- _(writer fills: 5-10 short, verifiable facts — default tab, currency format,
  language behavior, default sort, animation budget, cold-start time, etc.)_
- All values transcribed verbatim from device captures, no paraphrase.

---

## 3. Block A — {{block_a_name}} {#{{feature_slug}}/block/a}

### Layout

_(writer fills: header / body / footer regions, sticky vs scrollable)_

### Components

_(writer fills: list of distinct UI components on this block)_

### Interactions

| Tap target | Centre | Result |
|---|---|---|
| _(writer fills from observations transitions table)_ | _(x,y)_ | _(target screen anchor or behavior)_ |

### State changes

_(writer fills: any state mutations triggered by this block — eg. "tapping Start Plan
sets `enrolled=true` server-side, refreshes Today landing on next focus")_

---

## 4. Block B — {{block_b_name}} {#{{feature_slug}}/block/b}

_(repeat for each block C / D / E ...)_

---

## {{N}}. Navigation graph

```mermaid
flowchart TD
  %% Feature: {{feature_slug}} — auto-generated by `render_nav.py {{feature_slug}}`
  %% Replace this block by running: python tools/render_nav.py {{feature_slug}} -o spec/feature/{{feature_slug}}/{{feature_slug}}_nav.md
```

---

## {{N1}}. State machine

```mermaid
flowchart LR
  unbooted[unbooted] -- enter feature --> idle
  idle -- tap CTA --> active
  active -- complete --> done
  active -- back --> idle
```

(_writer fills if feature has lifecycle / progress / persistence_)

---

## {{N2}}. Observed bugs / quirks

- _(writer fills: enumerate UX bugs / inconsistencies / unclear copy / animation glitches.
  Each item MUST be reproducible — include exact step-list to repro.)_

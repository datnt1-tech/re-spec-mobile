---
feature: {{feature_slug}}
layer: flow
anchor: {{feature_slug}}/flow/root
title: {{app_name}} — {{feature_title}} Flow Spec
last_updated: '{{date_iso}}'
app_version: {{app_version}}
device: {{device_label}}
package: {{package}}
status: draft
blocks:
  - id: {{feature_slug}}/block/a
    letter: A
    name: "{{block_a_name}}"
    section_line: 60
    screens: [{{feature_slug}}/screen/{{screen_a_slug}}]
nav_edges:
  - from: {{feature_slug}}/screen/{{screen_a_slug}}
    to: {{feature_slug}}/screen/{{screen_b_slug}}
    trigger: "{{nav_trigger}}"
    block: {{feature_slug}}/block/b
states: []
related:
  - {{feature_slug}}/observations/root
  - {{feature_slug}}/implementation/root
---

# {{app_name}} — {{feature_title}} Flow Specification

**Package**: `{{package}}`
**App version**: `{{app_version}}`
**Activity**: `{{main_activity}}`
**Device tested**: {{device_label}}, locale `{{locale}}`
**Method**: mọi screen capture qua `capture.py` (a11y_tree Portal + screencap +
projection XML uiautomator-compat); mọi UI literal transcribe nguyên văn. Xem
`{{feature_slug}}_observations.md` cho bảng raw per-screen.

> Output viết bằng tiếng Việt, technical term giữ tiếng Anh (xem I18N_GLOSSARY).
> UI string trong backtick giữ NGUYÊN VĂN của app — không dịch.

---

## 1. Tổng Quan

{{feature_one_liner}}.

**Functional block** ({{block_count}} state UI khác biệt qua {{group_count}} nhóm chức năng):

| # | Block | States | Mục đích |
|---|---|---|---|
| A | {{block_a_name}} | {{block_a_states}} | {{block_a_purpose}} |
| B | {{block_b_name}} | {{block_b_states}} | {{block_b_purpose}} |

---

## 2. Hard facts before anything else

- _(writer fill: 5-10 fact ngắn, verifiable — tab mặc định, format currency,
  behavior ngôn ngữ, sort mặc định, animation budget, cold-start time, ...)_
- Mọi giá trị transcribe nguyên văn từ device capture, không paraphrase.

---

## 3. Block A — {{block_a_name}} {#{{feature_slug}}/block/a}

### Layout

_(writer fill: vùng header / body / footer, sticky vs scrollable)_

### Components

_(writer fill: list component UI khác biệt trên block này)_

### Interactions

| Tap target | Centre | Result |
|---|---|---|
| _(writer fill từ bảng transition observations)_ | _(x,y)_ | _(target screen anchor hay behavior)_ |

### State changes

_(writer fill: mọi mutation state mà block này trigger — vd. "tap Start Plan
set `enrolled=true` server-side, refresh Today landing ở focus tiếp")_

---

## 4. Block B — {{block_b_name}} {#{{feature_slug}}/block/b}

_(lặp cho mỗi block C / D / E ...)_

---

## {{N}}. Navigation graph

```mermaid
flowchart TD
  %% Feature: {{feature_slug}} — auto-generated bởi `render_nav.py {{feature_slug}}`
  %% Replace block này bằng cách chạy: re-spec-render-nav {{feature_slug}} -o spec/feature/{{feature_slug}}/{{feature_slug}}_nav.md
```

---

## {{N1}}. State machine

```mermaid
flowchart LR
  unbooted[unbooted] -- enter feature --> idle
  idle -- tap CTA --> active
  active -- complete --> done
  active -- back --> idle
```

(_writer fill nếu feature có lifecycle / progress / persistence_)

---

## {{N2}}. Bug / quirk đã quan sát

- _(writer fill: liệt kê UX bug / inconsistency / copy mơ hồ / glitch animation.
  Mỗi item PHẢI reproducible — kèm step-list chính xác để repro.)_
